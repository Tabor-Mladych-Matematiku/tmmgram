secret: "test"
admin_password: "admin"
timezone: 'Europe/Prague'
upload_folder: 'uploads'
allowed_files: 'image/jpeg,image/webp,image/png,image/heic'  # TODO: test heic on iPhone
# for debugging using a local SQLite database, remove the `db` section
db:
  user: "db_user"
  password: "db_password"
  hostname: "db_hostname"
  database: "db_database_name"